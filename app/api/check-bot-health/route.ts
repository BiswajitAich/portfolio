export async function GET() {
//     const API = process.env.NODE_ENV_BOT_API;
//     try {
//         if (!API) return new Response(JSON.stringify({ health: "notOk" }));
//         const res = await fetch(`${API}/health`);
//         if (res.ok) return new Response(JSON.stringify({ health: "ok" }));
//         else return new Response(JSON.stringify({ health: "notOk" }));
//     } catch (error) {
//         console.error((error as Error).message);
//         return new Response(JSON.stringify({ health: "notOk" }));
//     }
}